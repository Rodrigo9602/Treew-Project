import { Component } from '@angular/core';

/**
 * Componente que maneja la visualización de páginas de error.
 * Se utiliza para mostrar errores generales de la aplicación,
 * como páginas no encontradas o errores del servidor.
 */

@Component({
  selector: 'app-error',
  imports: [],
  templateUrl: './error.component.html',
  styleUrl: './error.component.scss'
})
export class ErrorComponent {
  // Este componente actualmente no tiene lógica implementada
  // pero está preparado para manejar diferentes tipos de errores
}
